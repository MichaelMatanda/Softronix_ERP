server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Softronix_ERP?autoReconnect=true&createDatabaseIfNotExist=true
    password: tmpaul123@R!
    username: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

